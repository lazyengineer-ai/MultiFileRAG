# MultiFileRAG
In this repo we will experiment with automating files into embedding using appropriate chunking strategy for an RAG agent to retrieve data from it. This will support multiple file types and input methods
